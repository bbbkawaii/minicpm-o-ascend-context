---
status: accepted
---

# 将两个赛道作为独立项目维护

仓库采用一个 GitHub 仓库、两个独立顶层项目的结构，而不是让创新应用依赖推理优化项目。这样会有少量配置重复，但能让不同协作者并行开发、独立测试和分别提交，避免赛道一的实验性改动破坏赛道二 Demo，也避免赛道二产品代码污染赛道一的性能复现包。

## Consequences

- 两个项目不得相互导入代码或依赖对方服务。
- 必要的模型服务适配、环境配置和启动脚本在各项目内分别维护。
- 跨项目复用优先通过上游开源项目完成，而不是在本仓库建立共享模块。
- 根目录只负责索引、规则与协作治理。
