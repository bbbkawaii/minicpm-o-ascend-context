# Reproduction commands

Use source commit `7a5a95a8` and the same service, c1/c4/c8, and full WER
commands recorded in experiment 007. The only configuration difference is
`token2wav_n_timesteps: 6`. Remote evidence root:
`/workspace/user_data/experiments/stage2-steps6-910c-20260811/`.
